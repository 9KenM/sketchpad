{
  "dependencies": {
    "express": "^4.18.2"
  },
  "scripts": {
    "serve": "node server.js",
    "publish": "git push origin :gh-pages && git subtree push --prefix public origin gh-pages && echo 'sketchpad.ken.today' > public/CNAME"
  }
}
